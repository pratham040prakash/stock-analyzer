openapi: 3.0.3
info:
  title: APEX Decision API
  version: 1.0.0
  description: Placeholder — see product/specifications/
paths:
  /v1/decisions/{id}:
    get:
      summary: Get decision
      responses:
        '200':
          description: Success
        '503':
          description: Degraded / offline
